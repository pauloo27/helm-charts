apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-java-api.fullname" . }}
  labels:
    {{- include "sample-java-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-java-api.selectorLabels" . | nindent 4 }}
